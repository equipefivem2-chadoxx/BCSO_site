<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.title || 'BCSO - Portail Partenaire' %></title>
    
    <link rel="icon" type="image/png" href="/images/bcso.png">
    
    <!-- Police Montserrat et Icônes FontAwesome -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        * { 
            margin: 0; 
            padding: 0; 
            box-sizing: border-box; 
        }
        
        body {
            background-color: #07090e;
            background-image: radial-gradient(circle at 50% 40%, rgba(189, 165, 129, 0.05), transparent 60%);
            color: #f3f4f6;
            font-family: 'Montserrat', sans-serif;
            overflow-x: hidden;
        }

        /* Gestion de l'espace central alignée sur les 280px de la sidebar */
        .main-content-wrapper {
            margin-left: 280px; 
            padding-bottom: 70px; /* Espace de sécurité pour ne pas cacher le bas sous le footer */
            min-height: 100vh;
            width: calc(100% - 280px); 
            padding-top: 40px; 
            padding-left: 40px; 
            padding-right: 40px; 
        }
    </style>
</head>
<body>

    <!-- SIDEBAR PARTENAIRE -->
    <%- include('../partials/sidebar-entreprise') %>
    
    <!-- ZONE DU CONTENU (DASHBOARD, ANNUAIRE...) -->
    <main class="main-content-wrapper">
        <%- body %>
    </main>

    <!-- BARRE DU BAS PARTENAIRE -->
    <%- include('../partials/header-entreprise') %>

    <!-- SCRIPT MAINTENANCE TEMPS RÉEL -->
    <script src="/socket.io/socket.io.js"></script>
    <script>
        const socket = io();
        socket.on('maintenance-toggled', (isMaintenance) => {
            if (window.location.pathname !== '/chadoxx') {
                window.location.reload(); 
            }
        });
    </script>
</body>
</html>