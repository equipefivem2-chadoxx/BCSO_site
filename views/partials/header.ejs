<style>
    .bcso-bottom-bar {
        position: fixed;
        bottom: 0;
        left: 280px; /* Aligné avec la largeur exacte de ta sidebar */
        right: 0;
        height: 50px;
        background-color: rgba(10, 14, 23, 0.98);
        backdrop-filter: blur(10px);
        border-top: 1px solid rgba(189, 165, 129, 0.2); 
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0 30px;
        z-index: 20;
        color: #6b7280;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 1px;
        font-family: 'Montserrat', sans-serif;
    }
    .brand-text {
        color: #bda581;
        font-weight: 800;
    }
    .bottom-right-section {
        display: flex;
        align-items: center;
        gap: 25px;
    }
    .btn-patchnote-bottom {
        background: rgba(189, 165, 129, 0.1);
        border: 1px solid rgba(189, 165, 129, 0.3);
        color: #bda581;
        padding: 6px 15px;
        border-radius: 6px;
        font-weight: 700;
        font-size: 0.7rem;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
    }
    .btn-patchnote-bottom:hover {
        background: #bda581;
        color: #05070b;
        box-shadow: 0 0 10px rgba(189, 165, 129, 0.3);
        transform: translateY(-2px);
    }
</style>

<div class="bcso-bottom-bar">
    <div><i class="fas fa-shield-alt"></i> <span class="brand-text">BCSO SYSTEM</span> - Réseau Sécurisé</div>
    
    <div class="bottom-right-section">
        <!-- NOUVEAU BOUTON PATCHNOTES -->
        <a href="/patchnotes" class="btn-patchnote-bottom"><i class="fas fa-bullhorn"></i> Patchnotes</a>
        
        <div>Statut : <span style="color: #10b981; font-weight: bold;">Connecté</span> | Session chiffrée</div>
    </div>
</div>

<!-- SCRIPT TEMPS RÉEL MAINTENANCE CHADOXX -->
<script src="/socket.io/socket.io.js"></script>
<script>
    const globalSocket = io();
    globalSocket.on('maintenance-toggled', (isMaintenance) => {
        if (isMaintenance) {
            window.location.reload(); 
        }
    });
</script>